{
  "compatibleVersion":"1.4",
  "pluginVersion":"1.5",
  "colors":[
    {{#each colors.palettes}}
      {{#each values}}"{{value}}"{{#unless @last}},{{/unless}}{{/each}}{{#unless @last}},{{/unless}}
    {{/each}}
  ]
}
